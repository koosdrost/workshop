---=== Workshop JSONB ===---

Readme:
Versions naar 1.0/1.1
Handige site: https://json-b.net/docs/user-guide.html#json-binding-api

Unexplained:
Static members are associated with the class, not the instances. So it does not make sense to include them when serializing an instance, therefore, they will be nulled.


# LAB1 (0.30 uur):
Objectvelden moeten expliciet public zijn om te lezen.
Volgorde van de velden in json is alfabetisch 
Null waardes worden default weggelaten in json

// TODO customize the way Jsonb will work... Wat bedoel je precies?

java.lang.IllegalAccessException: Class org.eclipse.yasson.internal.ReflectionUtils can not access a member of class workshop.binding.lab1.Boat with modifiers 
--> Object class heeft een default public constructor nodig voor een fromJson.

fromJson: ongematchte velden worden niet gevuld, default waarde wordt getoond.


# LAB2 (0.75 uur, ook omdat ik niet alles via annotaties deed ;)):
Erbij vermelden dat alles via een annotaties en bijbehorende params op te lossen valt.
EX1: Dacht dat je een locale op het veld moet zetten i.p.v. handmatig een waarde invoeren. Misschien verduidelijken voor de zekerheid.
De value param in @JsonbDateFormat moet volgens https://docs.oracle.com/javase/7/docs/api/java/text/SimpleDateFormat.html, dus MM != mm.